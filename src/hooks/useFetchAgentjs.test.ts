import { mount } from "enzyme";
import React from "react";
// import { useFetchAgent } from "./useFetchAgent";

// // Creating a dummy component that uses the hook
// const DummyComponent = () => {
//   const { loading, error, agent } = useFetchAgent();
//   if (loading) return <div>Loading...</div>;
//   if (error) return <div>Error: {error}</div>;
//   return <div>Agent: {agent.login.username}</div>;
// };

// describe('useFetchAgent', () => {
//   it('renders without crashing', () => {
//     // Note: you may need to mock API calls here.
//     mount(<DummyComponent />);
//   });

//   // Add other tests here as needed.
// });
